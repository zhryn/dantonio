extends /layouts/layout_works.pug

block title
    include /mixins/site_title.pug
    +site-title(title, "Work > Architecture > Mixed Use")

block body-menu
    include /components/menu.pug
    -
        const menu = makeCommonMenu(0)
        const submenu = makeSubCommonMenu(0, 1)

block body-data 
    include /components/data/work/architecture/mixed-use.pug
    -
        const items = [...mixedUse]
        const articleItems = [...items]
