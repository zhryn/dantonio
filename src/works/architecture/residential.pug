extends /layouts/layout_works.pug

block title
    include /mixins/site_title.pug
    +site-title(title, "Work > Architecture > Residential")

block body-menu
    include /components/menu.pug
    -
        const menu = makeCommonMenu(0)
        const submenu = makeSubWorkMenu(0, 2)

block body-data 
    include /components/data/work/architecture/residential.pug
    -
        const items = [...residential]
        const articleItems = [...items]
