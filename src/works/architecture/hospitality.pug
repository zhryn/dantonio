extends /layouts/layout_works.pug

block title
    include /mixins/site_title.pug
    +site-title(title, "Work > Architecture > Hospitality")

block body-menu
    include /components/menu.pug
    -
        const menu = makeCommonMenu(0)
        const submenu = makeSubWorkMenu(0, 3)

block body-data 
    include /components/data/work/architecture/hospitality.pug
    -
        const items = [...hospitality]
        const articleItems = [...items]
